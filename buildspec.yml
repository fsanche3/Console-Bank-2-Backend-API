  version: 0.2

  phases:
    build:
      commands:
        - mvn clean package

  post_build:
    commands:
      - mv target/*.jar ./console-bank-0.0.1-SNAPSHOT.jar

  artifacts:
    files:
      - console-bank-0.0.1-SNAPSHOT.jar
